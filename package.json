{
  "name": "xe-plugin",
  "version": "1.2.4",
  "description": "Just a starter WordPress plugin.",
  "scripts": {
    "init": "cd node_scripts && node init.js",
    "build": "cd node_scripts && node build.js",
    "blocks": "wp-scripts build --output-path=blocks",
    "serve": "wp-scripts start --output-path=blocks",
    "pot": "cd node_scripts && node pot.js",
    "docs": "mkdocs gh-deploy --force"
  },
  "license": "GPL-2.0-or-later",
  "devDependencies": {
    "@wordpress/scripts": "^26.2.0",
    "copy-dir": "^1.3.0",
    "replace-in-file": "^5.0.2",
    "rimraf": "^3.0.2",
    "wp-pot": "^1.9.9"
  }
}
